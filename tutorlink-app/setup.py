# Ce fichier fait le lien avec le vrai fichier de configuration
# 'setup.cfg'.
#
# Il faut qu'il soit là pour que 'pip install -e .' puisse fonctionner
# (cf. <https://setuptools.pypa.io/en/latest/setuptools.html#setup-cfg-only-projects>).
import setuptools
setuptools.setup()
